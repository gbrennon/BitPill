name: Check Commit Messages

on: [push, pull_request]

jobs:
  check-commits:
    runs-on: codeberg-medium
    steps:
      - uses: https://data.forgejo.org/actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Ensure full history
        run: bash scripts/ensure_full_history.sh

      - name: Check commit messages
        env:
          CI_EVENT_NAME: ${{ forgejo.event_name }}
          CI_BASE_REF: ${{ forgejo.event.pull_request.base_ref }}
          CI_HEAD_REF: ${{ forgejo.event.pull_request.head_ref }}
        run: bash scripts/check_commit_messages.sh
